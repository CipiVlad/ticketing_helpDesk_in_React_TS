import { useNavigate } from "react-router-dom";
import { Ticket } from "../models/ticket.model";

export default function handleDelteTicket(id: Ticket) {
    console.log(id);
    confirm('delte?')
    // navigate back to '/'


}